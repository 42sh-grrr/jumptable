#pragma once

/** Constants that configures the vulkan renderer and other classes
 */

namespace saltus::vulkan
{
    /// Max "FIF", the number of frames simultaneously rendered on the gpu
    const int MAX_FRAMES_IN_FLIGHT = 2;
}
